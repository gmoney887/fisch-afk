# Feature inventory and acceptance ledger

For future sessions: [prioritized AFK handoff](AFK-HANDOFF.md) records release versus local state, remaining acceptance requirements and where to resume. Its combined-tree caveat applies to the dated test snapshots below.

Updated 2026-09-20. This is the current working-tree review, not release approval. A passing test count does not establish reliable AFK operation. **Unattended operation is not accepted yet.**

Latest verification: 382 passed, zero failed, one existing private-fixture skip; overall coverage 57.97% lines / 49.49% branches, FishingEngine 69.39% / 55.54%. See [current coverage](COVERAGE-2026-09-20.md) and [overnight incident review](OVERNIGHT-REVIEW-2026-09-20.md). Death-screen waiting/Stop and core movement-key regressions pass. A separate bounded incident journal now retains recovery/outcomes/keyboard evidence independently of routine churn and is included in exports. Earlier measurements below remain historical. The overnight run failed effective operation despite nearly12 hours of uptime; death/position recovery and broader live gates remain open.

## Evidence and coverage

Death-identification follow-up supersedes the preceding 382-test snapshot: 392 passed, zero failed, one existing private-fixture skip. Overall coverage 58.33% lines / 49.68% branches ([report](COVERAGE-DEATH-2026-09-20.md)). Death now has a distinct waiting status, one incident event per continuous observation, prioritized image context, and an explicit failed-readiness explanation. Automatic respawn/position restoration and live acceptance remain open.

The earlier review covered individual components and selected images; it did not validate the complete controller or unattended recovery. The long run recorded 510 confirmed catches, 7 unknown outcomes and 5 recovery attempts over 6,546 seconds. Those are application counters, not independently labeled results. The retained image `artifacts/last-context.png` shows **“Game is shutting down for an update”**. A later live observation showed idle disconnect error 278. The evidence supports a server-update interruption followed by failed recovery; it does not establish idle prevention as the original cause.

Coverage measured with Coverlet on the Windows Release build after the shake jiggle, cast scenery and stacked catch corrections. Source run: `artifacts/stacked-catch-coverage/eb186bef082d4fa2b4f71640998b96e9/tests.trx`. The generated per-file report includes its Cobertura source path and SHA256 so later snapshots can be identified precisely.

| Measurement | Before this recovery follow-up | After |
|---|---:|---:|
| Tests reported passing / failed / skipped | 219 / 0 / 0 | 369 / 0 / 1 |
| Overall line coverage | 30.72% | 57.60% |
| Overall branch coverage | 25.81% | 49.04% |
| FishingEngine file, lines / branches | 7.31% / 2.58% | 69.29% / 55.17% |
| PreFlightDiagnostic file, lines / branches | 15.12% / 14.44% | 96.00% / 89.78% |
| AfkRecovery + heartbeat policies, lines / branches | absent | 100% / 97.44% |

File metrics include generated async/nested classes and merge duplicate source lines. Earlier class-only figures (FishingEngine 6.4%, diagnostic outer class 93.75%) are not comparable to file totals. Coverage measures executed code, not correctness or acceptance. Policy tests use simulated clocks/callbacks; they do not exercise Windows focus, actual input delivery, or Roblox's idle timer. Some tests inspect source/UI structure, so test counts also overstate behavioral breadth.

This review found four private-image tests could silently return without assertions when files were unavailable. They now resolve fixtures consistently across output directories and explicitly report missing private inputs as skipped. The final local run has one skip: `media_1789705793153.jpg` is absent. CI can have additional explicit skips because private captures are not committed. Part of the coverage increase comes from previously bypassed image tests actually executing, not from new behavioral tests. This makes the earlier passing count an unreliable acceptance measure. Build: zero warnings/errors; coverage script tested end to end. The combined build has passed a short live maximize/restore trial; details and limitations are recorded in LIVE-TESTING-2026-09-19.md.

Run `./scripts/Test-WithCoverage.ps1` from PowerShell. It runs tests, collects Cobertura/TRX, and generates `artifacts/coverage/summary.json` and `summary.md`. CI runs the same command and uploads the reports. Floors now require 40% lines / 34% branches overall and 45% lines / 35% branches in FishingEngine. These protect the new worker replays, not certify product acceptance. No production files are newly excluded to improve the numbers. Full per-file snapshots are in [COVERAGE-2026-09-19.md](COVERAGE-2026-09-19.md).

### Actual worker replay follow-up

Current scenario-by-scenario coverage and remaining goal requirements: [AFK-REGRESSION-MATRIX.md](AFK-REGRESSION-MATRIX.md). Actual-worker scenarios use the production GameplayInput guard with only final hardware delivery mocked. They cover missing/replaced windows, minimization, duplicate Start, shutdown while waiting, 26-minute simulated recovery with heartbeat on/off, all three shake modes, absent/occluded shake targets, navigation-key cancellation, server-update waiting, Reconnect and Continue actions. Current full suite:372 passed, one explicit missing-private-fixture skip, including chained Reconnect/Continue, repeated shake movement, cast scenery rejection, stacked catch rewards and a chronological recorded reel-to-catch transition; no live desktop input sent by these replays. The coverage table is the369-test measurement; historical results below retain their original scope.

`GameDesktop` isolates the engine's Windows calls while preserving the production Win32 adapter. `FishingEngineReplayTests` runs the actual coordinator, Start/Stop lifecycle, predictive casting, OpenCV reel/catch detection, controller transitions, outcome counters and recovery loop with a fake desktop, clock and input recorder. Eight scenarios cover confirmed and unknown catches through the next cast; two denied focus activations plus a transient capture failure and loading before recovery; and Stop during Casting, Luring, Reeling, PostCatch and recovery. Stop assertions reject gameplay input after cancellation. Tests fail if the required cycle/boundary is not reached before their bound.

Frames combine a synthetic cast bar/hotbar with existing recorded reel/catch crops. This is a controlled worker replay, not a complete chronological recording or a live Windows test. Inputs are recorded rather than delivered; native input guards have separate tests. Production desktop adapter coverage remains 0% by design of this offline suite, not by coverage exclusion. Before these tests engine coverage was 7.36% / 2.56%; afterward it is 48.33% / 39.81%. Hardware behavior, reconnect identity, geometry changes, stalls, real hotkeys and long-run memory still need acceptance evidence.

## Inventory

Worker follow-up: five additional scenarios cover width resize, DPI change, queued Stop followed by a new run on the same engine, a frozen visible reel, and sustained 120 ms reel captures. Resize/DPI assertions prohibit a new cast while the existing reel remains visible. Queued Stop must finalize the catch before stopping, then the next Start must clear stale queued intent and complete another cycle.

The slow-capture scenario failed before the fix: with a configured 1.8-second reel deadline, the worker remained in Reeling until the test stopped it at 20 simulated seconds. Invalid timing samples used `continue` before checking the reel timeout. The deadline now runs first; the test verifies exactly one Unknown, one watchdog recovery, and a subsequent confirmed catch. Velocity timing is reset at cycle transitions, and rejected timing samples yield one control tick. The fake clock now accounts for capture time. All 13 worker scenarios pass; these fixes have not yet been validated in a new live run.

Status **partial** means component evidence exists but the stated outcome is not accepted end to end. **Open** means a necessary behavior or acceptance test is missing. Every live trial must identify the executable/build, settings, viewport, recording ID, elapsed time, and observed result.

| ID / feature | Expected observable result | Implementation / automated evidence | Current result and remaining gap |
|---|---|---|---|
| F01 Start and ownership | One fishing worker; Start remains requested until explicit Stop; concurrent manual actions cannot inject competing input | FishingEngine, AutomationCoordinator; AutomationRuntimeTests, FishingEngineReplayTests | Partial. Actual Start → interruption → recovery → cycle → Stop replay passes. Concurrent manual-workflow/live OS matrix and exceptions outside recovery remain gaps. |
| F02 Stop / queued stop / restart | End releases input promptly; queued stop finishes current reel; old stop intent never affects a new run | FishingEngine, MainWindow; OptimizationRegressionTests, GameplayInputTests, FishingEngineReplayTests | Worker cancellation at all four cycle states and recovery passes. Queued-stop completion followed by same-engine restart now passes; no stale intent or held input. Physical hotkeys and live restart validation remain. |
| F03 Window discovery | Find supported Roblox client, wait while absent, reacquire after relaunch | Native/Win32; coordinate and worker tests | Worker retains Start while absent and resumes after a replacement window handle appears; no gameplay input during absence. Native desktop/UWP/multiwindow discovery still needs live validation. No automatic launch/join implemented. |
| F04 Focus / minimize / geometry | Restore foreground, avoid input elsewhere, reacquire dimensions/DPI, then resume verified gameplay | AfkRecovery, ValidateGameplay; AfkRecoveryTests, FishingEngineReplayTests | Worker tests pass for denied focus, capture interruption, width resize and DPI change; an active reel resumes without recasting. Windows can refuse foreground activation. One native UWP minimize/restore trial passes. The combined build also passes a short maximize/restore and catch-recognition trial at 96 DPI; other desktop/DPI configurations remain unaccepted. |
| F05 Capture visibility | Reject stale/occluded frames; no detection/input based on another app | ScreenCapture, CaptureVisibility, RecordingFrameSource; RecordingReplayTests | Partial. CaptureVisibility 14.29% line coverage. Layered click-through overlays handled; actual occluder/window matrix missing. |
| F06 Rod and hotbar | Verify hotbar geometry before equipping configured slot; absent hotbar keeps retrying | VisionProcessor, FishingEngine; RodDetectorTests, HotbarGeometryTests, DesktopHotbarTests | Partial. Saved/scaled frames pass; menu overlays and unusual inventories need negatives. |
| F07 Casting | Predictively release from observed bar; recover failed casts without ending request | FishingEngine; CastBarVisionTests, FishingEngineReplayTests, coordinate tests | Partial. Actual-worker predictive cast-to-lure replay now passes with synthetic bar input. Failed-cast retries and absent-bite recovery now pass; more fill velocities remain; “100% perfect” language is not supported by measured results. |
| F08 Shake modes | Navigation/Visual/Disabled behave as selected; detect reel promptly without extra clicks | FishingEngine, VisionProcessor; worker/vision tests | Worker cases pass for all modes, missing button, macro-occluded button, and Stop during key hold. Visual positive uses the shipped template; independent real-frame accuracy and equivalent long live trials remain. |
| F09 Reeling and themes | Follow the real bar, reject scenery; handle dim/clipped UI and theme changes; release input on exit | VisionProcessor, ReelEntryGuard; DimReelTests, ReelTrackingRegressionTests, MultiResolutionTests, FishingEngineReplayTests | Partial. Actual-worker replay with a recorded reel crop passes; selected dim/scenery fixtures pass. Moving chronological reels and rod/theme/background matrix remain. |
| F10 Catch confirmation | Count only verified catch banners across frames; absence remains Unknown | CatchOutcomeTracker, FishingCyclePolicy; CatchVerificationTests, OptimizationRegressionTests | Partial. Live-prefix fixtures pass. 7 unknowns in long run require independent labels; telemetry does not prove all 510 catches. |
| F11 Fast default cycle | Remove avoidable delays while respecting observed readiness; preserve cast prediction | AFK mode, FishingCyclePolicy; optimization tests | Partial. Earlier two PostCatch transitions measured 113/119 ms. Whole-session throughput gains not established. |
| F12 Watchdog and budgets | Detect nonprogress; release input; retry with cooldown indefinitely; only gameplay confirms recovery | RecoveryBudget, FishingRetryWait, AfkRecovery; retry/recovery and worker tests | Frozen-reel and sustained slow-capture timeouts recover and complete another catch. Fixed timing rejection bypassing the reel deadline. Same-thread watchdog cannot rescue a permanently blocked native call; no independent supervisor. |
| F13 Server update / reconnect | Detect connection state, rejoin if needed, wait through loading, resume same fishing request | AfkRecovery with reviewed disconnect/Continue identities, fresh revalidation and guarded input | **Open, P1 for live acceptance.** Reconnect attempts are at least10 seconds apart and Continue attempts at least2 seconds apart. Separate worker cases cover retry/Stop/stale prompts; chained worker cases now cover both screens with blank loading, focus loss and Stop during Continue. Initial Reconnect passed live; Continue required a manual click after Enter failed. The replacement click workflow and resumed fishing remain unaccepted live. |
| F14 Idle prevention | Periodic nonmoving input is accepted by Roblox even during a long recovery wait | AntiIdleHeartbeat, Win32InputSink; policy and worker tests | A 26-minute simulated worker wait verifies balanced, bounded F15 pulses when enabled, none when disabled, and eventual fishing resumption. Component failures/cancellation tested. Roblox acceptance still requires a >25-minute live trial. No “kick immunity” claim. |
| F15 Input cleanup | Focus/cancel/error releases all owned keys/buttons; one failed release does not prevent others | GameplayInput, Native/Win32; GameplayInputTests, native layout test | Partial. Every SendInput mouse/keyboard call checks delivery count. Cleanup attempts every release and retains rejected edges, including right-button up. Native click rechecks focus before pressing and attempts up in finally; WM_BUTTONUP is attempted even if SendInput fails. Five new regression cases cover rejected move/press, repeated right-up failures and incomplete delivery. Live OS rejection/focus behavior remains unverified; cursor positioning and supplementary window messages are not acceptance evidence. |
| F16 AFK performance toggle | Default on; suppress preview/jitter/topmost overhead and restore stored preferences when off | Settings, MainWindow; OptimizationRegressionTests | Component tests pass. Does not disable recording or automate game appearance settings. |
| F17 In-game visual clutter | Catch flags and distracting effects remain disabled across rejoin; no fishing controls hidden | Manually inspected Appearance settings; OPTIMIZATION-2026-09-19.md | Partial. Flags/other-player fish/lanterns/tints disabled, VFX hidden in prior session. Persistence after update/rejoin and macro control not implemented/tested. |
| F18 Aquarium claims | Confirm prerequisites and actual reward; missing evidence is Unknown; return to fishing | VerifiedWorkflow, FishingEngine; AutomationRuntimeTests | **Unavailable:** reviewed workflow templates missing. Off by default; not accepted and must not be reported successful. |
| F19 Crates | Select intended inventory item, verify opening/result, return safely to fishing | VerifiedWorkflow, FishingEngine; workflow tests | **Unavailable:** missing reviewed templates. Off by default. Prior window-not-found report is not evidence of successful opening. |
| F20 Statistics | Confirmed/failed/unknown remain distinct; reset and recording deltas agree; rate denominator explicit | FishingEngine, CatchOutcomeTracker; outcome/replay tests | **Open, P2.** WinRate excludes Unknown and defaults to 100% without known outcomes. Reset now clears UnknownCatches. Separate cumulative recording counters and monotonic duration preserve the complete recording across display resets; confirmed/Unknown worker replays pass. Reset while stopped leaves elapsed time at zero. |
| F21 Recording / failure evidence | Bounded storage, readable retained frame index, useful before/after failure context, explicit drops | FlightRecorder, BoundedEventJournal; RecordingReplayTests | Partial. Central recovery ROI and one preservation event per retry added. Historical journal references may point to expired images; use frame-index. Worker-level recovery recordings now verify metadata, readable retained frames, recovery events and completion statistics. Stop flush, disabled recording, writer failure and oversized-frame rejection are exercised. Sustained saturation and multi-hour failure evidence still need validation. |
| F22 Logs and performance | Bounded nonblocking producer, runtime rotation, shutdown flush; no growing native memory | SessionLogger, telemetry gates, Mat disposal; logger/telemetry/optimization tests | Component tests and local microbenchmarks pass. Multi-hour private-bytes trend and capture/encode p95/p99 absent. |
| F23 Diagnostics and replay | Preflight failure blocks unsafe start; replay provides reproducible decisions without live input | PreFlightDiagnostic, ReplaySession, ReplayWindow; corresponding tests | Partial. Actual diagnostic execution now covers missing windows, cancellation, context changes, missing hotbar and guarded equipment confirmation. File coverage94.30% lines/86.81% branches; native input/focus acceptance remains open. |
| F24 Settings and hotkeys | Persist preferences, recover corrupted config, honor one physical key edge; shortcuts match UI | Settings, GlobalKeyboardHook; KeyEdgeTracker/optimization tests | Partial. Edge helper tested, actual hook 0% lines; conflicting shortcuts and persistence failure need live/automated scenarios. |
| F25 Adaptive rod behavior | Opt-in only, bounded adjustments, persist only confirmed evidence and reject incompatible profiles | AdaptationProfile; AdaptationTests | Partial. Policy tests pass; live stability/benefit not established. Keep off for baseline acceptance. |
| F26 Reports / sharing | User reviews content; validate destination and credentials; export readable evidence and surface errors | PrivateReportClient, RecordingSubmissionService; report/submission tests | Partial. Mock/local tests exist; no real outbound submission performed in this review. |
| F27 Startup / shutdown / packaging | Start without duplicate worker; close flushes logs/recordings and releases input; portable launch works | App, MainWindow, worker tests, CI publish | Duplicate Start preserves a single worker; Dispose during a live unavailable-window wait cancels, joins, balances timers and prevents restart. WPF code-behind 0% coverage; portable launch and recording-under-load shutdown pending. No release requested. |

## Required acceptance scenarios

| Scenario | Pass condition | Evidence still required |
|---|---|---|
| A01 Normal cycle replay | Deterministic Start → cast → lure → reel → confirmed/unknown → next cast, with expected input edges | Controlled actual-worker replay passes for confirmed/Unknown outcomes, compact reels, stacked rewards and shake movement. Nine consecutive real frames cover moving reel → exit animation → player catch; they are not a complete chronological cycle. Complete recordings and additional themes/rods remain open. |
| A02 Persistent recovery | Loading/update, missing hotbar, focus loss, resize and capture failure never clear requested Start; no stale input; gameplay resumes | Worker focus denial, blank loading, transient capture failure, resize/DPI and slow/frozen reel recovery pass. One native minimize/restore and one short combined-build maximize/restore trial pass. Real update/reconnect and broader Windows configurations remain pending. |
| A03 Disconnect | True Reconnect dialog recognized across scales; other dialogs rejected; bounded retries; resume only after gameplay | Initial Reconnect click passed live. Continue detection passed, but repeated Enter did not advance the real prompt despite passing worker replay. A stopped-worker mouse probe advanced to gameplay; replacement verified-prompt clicking requires live acceptance. Resumed fishing and original-position recovery remain unproven. |
| A04 Idle | >25 minutes without manual input or normal fishing input, heartbeats accepted, no idle disconnect | Simulated 26-minute worker scheduling/persistence passes with input guard. Live foreground checks, heartbeat acceptance and final screen still required; counters alone are insufficient. |
| A05 Cancellation | Stop at every state and recovery stage prevents further down/click edges and releases every owned input | Stop passes at every cycle state and recovery, including held shake movement; queued-stop/restart and rejected releases pass. New recordings retain the first stop source through cleanup and clear it on Start. User confirmed the latest live stop was intentional. Physical hotkey validation remains pending. |
| A06 Soak | At least two hours, independently sample caught/unknown events; no silent stop, stuck state or unbounded memory/storage | Earlier109-minute run failed. Jiggle build completed31 minutes before deliberate replacement:118 confirmed/3 Unknown; all three independently reviewed as missed real player catches. Corrected build reached21 confirmed/0 Unknown over337.914s, then user stopped it. Neither trial meets two hours; fishing remains off. |
| A07 Performance | Compare same replay/settings before/after; lower latency/allocations without worse detection/outcomes | Current paired annotation/recording comparison covers2880 frames, identical predictions,0 recorder drops, two reverse-order passes. Timing ranges overlap; no speedup claimed. Native capture/input, WPF rendering, full-worker before/after performance and catch-rate benefit remain open. See OPTIMIZATION-2026-09-19.md. |

## Work order and definition of done

1. Validate the complete reconnect workflow. Initial Reconnect passed live; Continue Enter failed and was replaced with verified prompt clicking after a successful isolated mouse probe. Post-join readiness, resumed fishing and position restoration remain open. Native SendInput result handling and input-release failure isolation are regression-tested.
2. Extend worker replays to later optional workflow steps, persistent overlays and sustained recording saturation. Failed casts/absent bites, recovery cooldown exhaustion, six hardware rejection scenarios and missing optional prerequisites now pass. Missing/replaced windows, minimization, shutdown while waiting, shake modes, resize, stalls and restart now have passing scenarios. See the regression matrix for exact remaining requirements.
3. Run A03/A04 with the exact new executable, then A06. Label every failure and preserve its evidence before calling AFK reliable.
4. Correct statistics/reset semantics and unsupported UI claims. Validate optional workflows separately; do not let them invalidate core-fishing acceptance.
5. Profile and optimize the measured hot paths after the correctness gates pass.

For each behavioral change: update the affected feature row, add a reproducer that fails before the fix where practical, run all tests and coverage, attach scenario evidence, and keep unresolved live checks visible. Neither 100% coverage nor a green CI run can override a failed live acceptance scenario.

Latest worker additions: failed casts, absent bites, recovery burst/cooldown and six hardware rejection cases retain Start and eventually catch. Aquarium/crate missing prerequisites are Unknown, attempted once, then skipped through a second catch. Workflow template lookup is injectable for deterministic tests; temporary vision instances are disposed. The lower-right shake screenshot exposed overly narrow search bounds, now widened and covered by five recorded-button scenarios. Live validation remains pending.








Optional-workflow follow-up: six worker scenarios now cover the first action timing out, hardware rejecting that action, and Stop during the press, for both crates and aquarium. Production template matching uses generated test-only templates. A rejected crate click previously retried because the skip flag was set only on a returned failure; flags now stay set across exceptions and clear only on confirmed success. Aquarium outcome resets to Unknown before execution. Real menu/reward recognition remains unavailable without reviewed fixtures.




Queued user research after regression testing: investigate AFK movement away from the original fishing position into water. Cause is unknown per user; begin with recording and input evidence. Distinguish macro input, knockback, moving geometry, respawn and teleport; evaluate position holding/return-to-spot with verified fishing readiness. No position restoration currently exists.






Later-workflow regression: crates-overlay and aquarium-overlay complete the first navigation step, fail the second action without reward evidence, retain a visible menu for three simulated seconds of recovery, and prohibit gameplay input until its hotbar returns. Both finish a second catch and skip further optional attempts. Synthetic templates exercise production matching and control sequencing; automatic menu dismissal and real reward identity remain unaccepted.



Test infrastructure follow-up: default application data can be isolated by a process-local AppContext host override. Regression startup sets it to a unique temporary directory before Settings/SessionLogger initialization; a test verifies settings, logs and recording paths. Normal desktop startup leaves the override unset and retains the existing LocalApplicationData/FischAFKPro path. This does not alter fishing behavior in the running soak build.

Settings recovery regressions: malformed JSON and null now use a valid backup with the same legacy normalization as the primary path. Subsequent Save never replaces the backup with a damaged primary. Invalid primary evidence remains preserved, including when the backup is also invalid. Valid save/backup round-trip is tested in isolated test storage. These startup/persistence fixes are not yet in the running soak executable; fishing control is unchanged.


Live false-exit regression: frame 5202 has a compact progress layout (higher vertical band and narrower centered width) and a shorter dim control bar. Detection now includes both recorded progress layouts and the observed dim-bar height. Positive 1353/1080/720-height fixtures, progress-erased negative, existing scenery negatives and a complete worker cycle exercise this change. New live acceptance must use the updated build.





Latest regression run:316 passed,0 failed,1 existing skip (server-update-full-tests). The coverage table above remains the314-test coverage snapshot taken before the UI follow-up and these two scenarios; percentages were not remeasured. Readiness audit: PreFlight_ReportStepList_ContainsAllExpectedDiagnosticProbes constructs its own report and does not exercise RunDiagnosticAsync. Actual diagnostic orchestration coverage remains a gap despite helper tests.

Readiness execution follow-up supersedes the preceding readiness audit: the report-list test now runs the diagnostic, and16 new execution cases pass. Two reproduced cancellation gaps were fixed before window discovery and final snapshot capture. Full suite332 passed,0 failed,1 existing skip; coverage56.25% lines/48.25% branches overall and94.30%/86.81% for PreFlightDiagnostic. Evidence: artifacts/coverage/adfe5c496c434b83b929e7bf74ddb66f/tests.trx. Corrected build artifacts/preflight-guard-build, SHA25668071DDAC19D7A68ACD18B157ADF7BD920FF701E37E2B27B34938457354CEEA3, zero warnings/errors. This build has not been launched; no new live AFK acceptance claimed.
